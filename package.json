{
  "name": "yarnworks",
  "version": "0.1.0",
  "description": "A virtual knitter: parses knitting patterns, simulates the fabric, and renders it in 3D.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "python3 -m http.server 8000"
  }
}
